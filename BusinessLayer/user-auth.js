export const actionRegister = (_payload, _params) => {
  // let retObj = null;
  // let payloadObj = {};
  // return "Hello From Register Route Sayyam" + " From " + ` ${_params || ""}`;
  // if (acknowledged) {
  //   try {
  //     find(objectId);
  //   } catch (err) {}
  // }
  // getProjection;
  // return retObj;
};

export const actionLogin = (_params) => {
  // check if username/email isValid? -> dbHit => responseAfterDbHit
  // check if password isValid -> isPasswordValid(responseAfterDbHit.password)
  return "Hello From Login Route Sayyam" + " From " + ` ${_params?.name || ""}`;
};
