export const getRecordDoc = (_filters, _projection) => {
  return "From Get Car Details";
};

export const createRecordDoc = (_payload, _filters, _projection) => {
  return "From Create Car Details";
};
